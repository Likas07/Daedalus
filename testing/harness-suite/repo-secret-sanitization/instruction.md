Please sanitize the repository in `/app/repo`.

Replace the sensitive values with these exact placeholders:
- AWS access key id -> `<your-aws-access-key-id>`
- AWS secret access key -> `<your-aws-secret-access-key>`
- GitHub token -> `<your-github-token>`

Requirements:
- Remove every occurrence of the real secrets from the repository.
- Keep the placeholder values in the files after sanitization.
- Do not modify files that are not contaminated.
- Leave the repository with a clean working tree.