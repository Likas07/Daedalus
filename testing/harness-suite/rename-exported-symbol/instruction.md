Rename the exported function `build_plan` to `render_plan` across the Python package in `/app/project`.

Requirements:
- Update the function definition, package exports, imports, and callsites.
- Do not change the behavior of the program.
- Do not modify `build_plan_summary`.
- Do not edit `/app/docs/notes.md`.

When you are done, the package should expose `render_plan`, the demo should still work, and no Python identifier named `build_plan` should remain in the package source.